/* =====================================================================
   AXISTENCE - dashboard.js
   Logica propia del apartado Panel principal (dashboard.php).
   Por ahora el panel esta vacio; aqui iran sus widgets/resumenes
   (contadores, alertas de vencimientos, actividad reciente, etc.).
   ===================================================================== */

$(function () {
    "use strict";
    // Sin comportamiento por ahora.
});
